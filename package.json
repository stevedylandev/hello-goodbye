{
	"name": "hello-goodbye",
	"version": "0.0.0",
	"private": true,
	"description": "Monorepo workspace for hello projects",
	"type": "module",
	"packageManager": "pnpm@9.15.2",
	"scripts": {
		"changeset": "changeset",
		"bump:stable": "changeset version",
		"bump:next": "changeset version --snapshot",
		"release:stable": "changeset publish",
		"release:next": "pnpm publish -r --access public --tag next",
		"packages:prepublish": "pnpm -r prepublish"
	},
	"devDependencies": {
		"@changesets/changelog-github": "^0.5.2",
		"@changesets/cli": "^2.29.8"
	},
	"peerDependencies": {
		"typescript": "^5"
	},
	"engines": {
		"node": ">=14"
	}
}
